<nav>
    <ul>
        <li><a href="{{ path('main_home') }}">Home</a></li>
        <li><a href="{{ path('main_aboutUs') }}">About us</a></li>
        <li><a href="{{ path('wish_list') }}">All wishes</a></li>
        {% if app.user %}
            <li><a href="{{ path('wish_add') }}">Add wish</a></li>
            {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('admin_dashboard') }}">Admin dashbord</a></li>
            {% endif %}
            <li><a href="{{ path('app_logout') }}">Logout {{ app.user.pseudo }}</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}">Login</a></li>
            <li><a href="{{ path('app_register') }}">Register</a></li>
        {% endif %}
    </ul>
</nav>